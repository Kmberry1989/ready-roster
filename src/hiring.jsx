import React, { useMemo, useState } from 'react';
import { BriefcaseBusiness, Copy, FileText, Send, UserPlus } from 'lucide-react';

const field = 'mt-1 w-full rounded-lg border border-slate-200 bg-white p-3 text-sm outline-none focus:border-indigo-500';
const stages = ['new', 'reviewing', 'phone_screen', 'interview', 'offer', 'hired', 'rejected'];
const applicationUrl = (jobId) => `${window.location.origin}${window.location.pathname}?apply=${encodeURIComponent(jobId)}`;

export function HiringPanel({ organization, jobs = [], applications = [], onCreateJob, onUpdateApplication, onInviteApplicant }) {
  const [job, setJob] = useState({ title: '', department: '', location: '', employmentType: 'Full time', description: '', requirements: '' });
  const [filter, setFilter] = useState('all');
  const visible = useMemo(() => applications.filter(item => filter === 'all' || item.stage === filter), [applications, filter]);
  const copy = async value => { await navigator.clipboard?.writeText(value); };
  return <section className="mt-6 rounded-2xl border border-indigo-100 bg-white p-6 shadow-sm"><div className="flex items-center gap-3"><BriefcaseBusiness className="text-indigo-600"/><div><p className="text-xs font-bold uppercase tracking-widest text-indigo-600">Hiring workspace</p><h2 className="text-2xl font-black">Applications & applicant pool</h2></div></div><div className="mt-6 grid gap-7 xl:grid-cols-[.85fr_1.15fr]"><div><h3 className="font-black">Create an application</h3><form onSubmit={async event => { event.preventDefault(); await onCreateJob(job); setJob({ title: '', department: '', location: '', employmentType: 'Full time', description: '', requirements: '' }); }} className="mt-3 space-y-3 rounded-xl bg-slate-50 p-4"><input required className={field} placeholder="Job title" value={job.title} onChange={event => setJob({ ...job, title: event.target.value })}/><div className="grid gap-3 sm:grid-cols-2"><input className={field} placeholder="Department" value={job.department} onChange={event => setJob({ ...job, department: event.target.value })}/><input className={field} placeholder="Location or remote" value={job.location} onChange={event => setJob({ ...job, location: event.target.value })}/></div><select className={field} value={job.employmentType} onChange={event => setJob({ ...job, employmentType: event.target.value })}><option>Full time</option><option>Part time</option><option>Seasonal</option><option>Contract</option></select><textarea required className={field} placeholder="Role overview and responsibilities" value={job.description} onChange={event => setJob({ ...job, description: event.target.value })}/><textarea className={field} placeholder="Requirements and qualifications" value={job.requirements} onChange={event => setJob({ ...job, requirements: event.target.value })}/><button className="w-full rounded-lg bg-indigo-600 py-3 font-bold text-white">Publish application link</button></form><h3 className="mt-6 font-black">Open applications</h3><div className="mt-3 space-y-2">{jobs.filter(job => job.status === 'open').map(item => <div key={item.id} className="rounded-lg border p-3"><p className="font-bold">{item.title}</p><p className="text-sm text-slate-500">{item.department || 'General'} · {item.location || 'Location not specified'}</p><div className="mt-3 flex gap-2"><button onClick={() => copy(applicationUrl(item.id))} className="rounded border px-3 py-2 text-xs font-bold"><Copy className="mr-1 inline" size={14}/>Copy link</button><a href={applicationUrl(item.id)} target="_blank" rel="noreferrer" className="rounded border px-3 py-2 text-xs font-bold">Preview</a></div></div>)}{jobs.length === 0 && <p className="text-sm text-slate-500">Publish a role to start collecting applications.</p>}</div></div><div><div className="flex flex-wrap items-center justify-between gap-3"><div><h3 className="font-black">Applicant pool</h3><p className="text-sm text-slate-500">Track contact details, availability, experience, interview notes, and a defensible stage history.</p></div><select className="rounded border p-2 text-sm" value={filter} onChange={event => setFilter(event.target.value)}><option value="all">All stages</option>{stages.map(stage => <option key={stage} value={stage}>{stage.replaceAll('_', ' ')}</option>)}</select></div><div className="mt-4 space-y-3">{visible.map(applicant => <article key={applicant.id} className="rounded-xl border p-4"><div className="flex flex-wrap justify-between gap-3"><div><p className="font-black">{applicant.name}</p><p className="text-sm text-slate-500">{applicant.email} · {applicant.phone || 'No phone'} · {jobs.find(job => job.id === applicant.jobId)?.title || 'Application'}</p><p className="mt-2 text-sm text-slate-700">{applicant.summary || 'No professional summary supplied.'}</p>{applicant.availability && <p className="mt-1 text-xs text-slate-500">Availability: {applicant.availability}</p>}</div><select className="h-fit rounded border p-2 text-sm" value={applicant.stage || 'new'} onChange={event => onUpdateApplication(applicant.id, { stage: event.target.value })}>{stages.map(stage => <option key={stage} value={stage}>{stage.replaceAll('_', ' ')}</option>)}</select></div><div className="mt-3 grid gap-2 sm:grid-cols-[1fr_auto]"><input defaultValue={applicant.interviewAt || ''} type="datetime-local" className="rounded border p-2 text-sm" onBlur={event => onUpdateApplication(applicant.id, { interviewAt: event.target.value })}/><button onClick={() => onInviteApplicant(applicant.email)} className="rounded bg-emerald-600 px-3 py-2 text-xs font-bold text-white"><UserPlus className="mr-1 inline" size={14}/>Invite as employee</button></div><textarea defaultValue={applicant.reviewerNotes || ''} onBlur={event => onUpdateApplication(applicant.id, { reviewerNotes: event.target.value })} className="mt-2 w-full rounded border p-2 text-sm" placeholder="Private reviewer notes"/></article>)}{visible.length === 0 && <div className="rounded-xl border border-dashed p-8 text-center text-sm text-slate-500">No applicants in this view yet.</div>}</div></div></div></section>;
}

export function ApplicantPortal({ profile, jobs, onSubmit }) {
  const jobId = new URLSearchParams(window.location.search).get('apply') || profile.applicationTargetJobId;
  const job = jobs.find(item => item.id === jobId);
  const [form, setForm] = useState({ phone: '', location: '', availability: '', summary: '', experience: '', education: '', workAuthorized: '', resumeUrl: '' });
  const [sent, setSent] = useState(false);
  if (!job) return <main className="min-h-screen bg-slate-50 p-6"><div className="mx-auto mt-20 max-w-xl rounded-2xl bg-white p-8 text-center shadow"><FileText className="mx-auto text-indigo-600" size={36}/><h1 className="mt-4 text-2xl font-black">Application link unavailable</h1><p className="mt-2 text-slate-600">Ask the organization for a current application link.</p></div></main>;
  return <main className="min-h-screen bg-slate-50 p-5"><section className="mx-auto my-8 max-w-3xl rounded-2xl border bg-white p-7 shadow-sm"><p className="text-sm font-bold uppercase tracking-widest text-indigo-600">Employment application</p><h1 className="mt-1 text-3xl font-black">{job.title}</h1><p className="mt-2 text-slate-600">{job.department || 'General'} · {job.location || 'Location not specified'} · {job.employmentType}</p><p className="mt-5 whitespace-pre-wrap text-slate-700">{job.description}</p><form onSubmit={async event => { event.preventDefault(); await onSubmit({ ...form, jobId: job.id }); setSent(true); }} className="mt-7 space-y-4"><div className="grid gap-4 sm:grid-cols-2"><label className="text-sm font-bold">Name<input disabled value={`${profile.firstName || ''} ${profile.lastName || ''}`.trim()} className={field}/></label><label className="text-sm font-bold">Email<input disabled value={profile.email || ''} className={field}/></label><label className="text-sm font-bold">Phone<input required value={form.phone} onChange={event => setForm({ ...form, phone: event.target.value })} className={field}/></label><label className="text-sm font-bold">City / region<input value={form.location} onChange={event => setForm({ ...form, location: event.target.value })} className={field}/></label></div><label className="text-sm font-bold">Availability<input required value={form.availability} onChange={event => setForm({ ...form, availability: event.target.value })} className={field} placeholder="Earliest start date and typical availability"/></label><label className="text-sm font-bold">Professional summary<textarea required value={form.summary} onChange={event => setForm({ ...form, summary: event.target.value })} className={field}/></label><label className="text-sm font-bold">Relevant experience<textarea value={form.experience} onChange={event => setForm({ ...form, experience: event.target.value })} className={field}/></label><label className="text-sm font-bold">Education or credentials<textarea value={form.education} onChange={event => setForm({ ...form, education: event.target.value })} className={field}/></label><label className="text-sm font-bold">Resume / portfolio URL<input type="url" value={form.resumeUrl} onChange={event => setForm({ ...form, resumeUrl: event.target.value })} className={field}/></label><label className="text-sm font-bold">Work authorization<select required value={form.workAuthorized} onChange={event => setForm({ ...form, workAuthorized: event.target.value })} className={field}><option value="">Select an answer</option><option value="authorized">Authorized to work</option><option value="requires_sponsorship">Requires sponsorship</option><option value="other">Prefer to discuss</option></select></label><button disabled={sent} className="flex w-full items-center justify-center gap-2 rounded-lg bg-indigo-600 py-3 font-bold text-white disabled:opacity-50"><Send size={16}/>{sent ? 'Application submitted' : 'Submit application'}</button></form></section></main>;
}
